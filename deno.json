{
  "tasks": {
    "build": "cd src-rust; cargo build",
    "dev:run-fast": "export RUST_LIB_PATH=$CARGO_TARGET_DIR/debug && deno run --unstable -A example.ts",
    "dev": "deno task build && deno task dev:run-fast",
    "fmt": "deno fmt; cd src-rust; cargo fmt"
  }
}

